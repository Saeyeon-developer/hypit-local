interface:
  display_name: "Hypit"
  short_description: "Analyze videos and create prompts, or produce an explicit video brief"
  default_prompt: "Use $hypit to analyze my supplied video locally and create evidence-backed prompts; only generate or render when I explicitly ask for production."
